#include "syscall.h"

int main() {
    // int tmp, n, m;
    // int array[100];

    // PrintString("Enter number of elements: ");
    // n = ReadNum();
    // while (n <= 0 || n > 100) {
    //     PrintString("\nError, please try again: ");
    //     n = ReadNum();
    // }

    // PrintString("\nInput array[]: ");
    // for (int i = 0; i < n; i++) {
    //     array[i] = ReadNum();
    // }

    // PrintString("\n\t-----Menu-----\n");
    // PrintString("1) Ascending\n");
    // PrintString("2) Descending\n");
    // PrintString("Please enter your choice (1 or 2): ");
    
    // m = ReadNum();
    // while (m != 1 && m != 2) {
    //     PrintString("Error, please try again: ");
    //     m = ReadNum();
    // }
    
    // PrintString("\nYour array: ");
    // for (int i = 0; i < n; i++) {
    //     PrintNum(array[i]);
    //     PrintChar('\t');
    // }

    // if (m == 1) {
    //     for (int i = 0; i < n - 1; i++) {
    //         for (int j = 0; j < n - i - 1; j++) {
    //             if (array[j] > array[j + 1]) {
    //                 tmp = array[j];
    //                 array[j] = array[j + 1];
    //                 array[j + 1] = tmp;
    //             }
    //         }
    //     }
    // }
    // else if (m == 2) {
    //     for (int i = 0; i < n - 1; i++) {
    //         for (int j = 0; j < n - i - 1; j++) {
    //             if (array[j] < array[j + 1]) {
    //                 tmp = array[j];
    //                 array[j] = array[j + 1];
    //                 array[j + 1] = tmp;
    //             }
    //         }
    //     }
    // }
    
    // PrintString("\nYour array after sorting: ");
    // for (int i = 0; i < n; i++) {
    //     PrintNum(array[i]);
    //     PrintChar('\t');
    // }
    // return 0;
}